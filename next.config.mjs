/** @type {import('next').NextConfig} */
const config = {
    experimental: {
    },
    images: {
      domains: ['localhost','shabby-backend.vercel.app','shabby-backend.onrender.com','livebakend.onrender.com'],
      // domains: ['shabby-backend.vercel.app'],
    },
  };
  
  export default config;
  